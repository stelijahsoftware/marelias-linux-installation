// Install another language:
sudo gnome-language-selector
right click EN icon in system tray > preferences > Input method > Add > Arabic > Arabic ACERTY Eastern numerals

// Language/keyboard on ubuntu:
If you can't use any keyboard shortcuts in awesome (because keyboard started as arabic), left click on the language icon in taskbar (saying 'EN'), and select english.

